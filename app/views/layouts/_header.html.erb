<nav class="header">
<div class="container">
  <div class="row">
    <div class="col-sm-4">
        <% if current_user %>
            <% if current_user.nickname == nil || current_user.nickname == "" %>
                <h5> Welcome <%= link_to current_user.email, welcome_path(current_user.id) %></h5>
            <% else %>
                <h5>Welcome <%= link_to current_user.nickname, welcome_path(current_user.id) %></h5>
            <% end %>  
        <% else %> 
            <a href="/"> RECIPE </a>
        <% end %>      
    </div>
    <div class="col-sm-4"></div>
    <div class="col-sm-4 text-right">
        <% if current_user %>
            <div class="col-sm-12">  <p><%= link_to("Sign out", destroy_user_session_path, :method => 'delete') %></p> </div>
        <% else %>
            <div class="col-sm-6">  <p><%= link_to("Sign In", new_user_session_path) %><br></p> </div>
            <div class="col-sm-6">  <p><%= link_to("Sign Out", destroy_user_session_path, method: :delete) %><br><br></p> </div>
        <% end %>
    </div>
</div>
</nav>



  
    

